export interface FbResponse {
    name: string,
}

export interface Product {
    type?: string,
    id?: string,
    title?: string,
    photo?: string,
    price?: string,
    info?: string,
    date?: Date
}